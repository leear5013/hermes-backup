terminal:
  backend: local
  cwd: /data/workspace
  timeout: 180
web:
  search_backend: ddgs
compression:
  enabled: true
  threshold: 0.85
command_allowlist:
  - execute_code
onboarding:
  seen:
    profile_build_offered: true
    busy_input_prompt: true
_config_version: 33
